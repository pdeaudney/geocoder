[package]
name = "wof-importer"
version = "0.1.0"
edition = "2021"
description = "Import Who's on First country-level polygons as a fallback for Geofabrik-extract-missing admin_level=2 boundaries."
license = "Apache-2.0"
publish = false

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
serde_json = "1"

[[bin]]
name = "wof-importer"
path = "src/main.rs"
