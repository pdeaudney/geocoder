[package]
name = "index-dumper"
version = "0.1.0"
edition = "2021"
description = "Dump the mmap'd geocoder index to CSV so DuckDB can query it."
license = "Apache-2.0"
publish = false

[dependencies]
query-server = { path = "../../server", default-features = false }

[[bin]]
name = "index-dumper"
path = "src/main.rs"
