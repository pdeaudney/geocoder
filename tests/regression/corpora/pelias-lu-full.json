{
  "name": "pelias-lu-full",
  "description": "Converted from pelias/acceptance-tests (pelias-combined). Only cases with an extractable locality/region and status != 'fail' are emitted. Expectation rules: contains_ci on locality/region/name, country_code filter when expected country_a is known.",
  "defaults": {
    "auth_key": "REGRESSION_TEST_TOKEN"
  },
  "cases": [
    {
      "expect": {
        "checks": [
          {
            "kind": "json_path_min_count",
            "min": 1,
            "path": "results"
          }
        ],
        "status": 200
      },
      "id": "pelias-pelias-combined-wof_countries-7",
      "notes": "placeholder supports alpha3 and alpha2 country codes",
      "request": {
        "method": "GET",
        "path": "/search",
        "query": {
          "country_code": "lu",
          "q": "LUX"
        }
      },
      "tags": [
        "pelias",
        "search"
      ]
    },
    {
      "expect": {
        "checks": [
          {
            "kind": "json_path_min_count",
            "min": 1,
            "path": "results"
          }
        ],
        "status": 200
      },
      "id": "pelias-pelias-combined-wof_countries-7.1",
      "notes": "placeholder supports alpha3 and alpha2 country codes, Luxembourg is returned as a locality",
      "request": {
        "method": "GET",
        "path": "/search",
        "query": {
          "country_code": "lu",
          "q": "LUX"
        }
      },
      "tags": [
        "pelias",
        "search"
      ]
    }
  ]
}
